import styled from 'styled-components';

export const Form = styled.form`
    width: 100%;
    max-width: 720px;
    padding: 2rem;
    height: auto;

    > button {
        margin-left: .5rem;
    }
`
